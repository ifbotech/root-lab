{
  "name": "root-lab",
  "version": "0.1.0",
  "description": "La app y la nube de ROOTKIT: vinculación por QR, cofre, identificación por foto, tablero de plantas y notificaciones.",
  "type": "module",
  "private": true,
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning server/index.mjs",
    "dev": "node --disable-warning=ExperimentalWarning --watch server/index.mjs",
    "test": "node --disable-warning=ExperimentalWarning --test test/*.test.mjs",
    "firmware": "node tools/sincronizar-firmware.mjs",
    "verificar": "node tools/verificar-despliegue.mjs"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  }
}
